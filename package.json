{
  "name": "uptime-monitor",
  "version": "1.0.0",
  "description": "Zero-dependency uptime monitor",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "dev": "node --experimental-strip-types --env-file-if-exists=.env --watch index.ts",
    "start": "node --env-file-if-exists=.env --watch dist/js/index.js",
    "build": "tsc"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "nodemailer": "^7.0.11"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/nodemailer": "^7.0.4",
    "typescript": "^5.7.2"
  }
}
