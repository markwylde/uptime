SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_TLS="true"
SMTP_USER="user"
SMTP_PASSWORD="pass"
SMTP_FROM_NAME="Uptime"
SMTP_FROM_ADDRESS="noreply@example.com"
SMTP_ENABLED="true"

# Logging configuration
# Set LOG_FILE to write all logs (including console.log) to a file
# Leave empty or unset to write to stdout with pretty formatting
# LOG_FILE="/var/log/uptime.log"
# LOG_LEVEL="info"
